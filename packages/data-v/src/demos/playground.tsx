import { useState, useMemo, useCallback } from "react";
import { Card, Input, Button, message } from "antd";
import { ProCard } from "@ant-design/pro-components";
import { Stage } from "@q25a25q/data-v";
import { css } from "@emotion/css";

export default () => {
  return (
    <div>
      <Stage />
    </div>
  );
};
